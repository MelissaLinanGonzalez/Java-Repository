/*Pedir el radio de un círculo y calcular su área. A=pi*r^2 */
import java.io.BufferedReader;
import java.io.BufferedInputStream;
import static java.lang.Math.PI;

public class area_circulo {
    public static void main(String[] args) {
        double a, r; //área y radio
        System.out.println("Introduce el radio de un círculo: ");
        //r = Entrada.real();

        //a = Math.PI*Math.pow(r,2); //pow es elevar el primer elemento el segundo
        //a = Math.PI*(r*r); Otra opción

        //System.out.println("El área de una circunferencia de radio " + r + "es: " + Math.round(a*100.0) / 100.0 + " metros cuadrados");
    }

}
